// Stampa il cubo dei primi N numeri, dove N è un numero indicato dall’utente. ( non ho capito benissimo la richiesta)

const userNumber = prompt("Inserisci un numero");

const cubo = userNumber * userNumber * userNumber;
console.log(cubo);
